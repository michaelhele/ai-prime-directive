# Project handover

This is a handover document, not standing context. It works like a shift change in a hospital: the incoming agent (or human) reads it once at the start of a session to get up to speed, then works from their own understanding. Do not import it or reload it during a session.

**Rules for this file (do not delete this block):**
- The current-state summary below is fully rewritten at the end of every session. Hard cap: 20 lines. If it will not fit, it is too detailed; the detail belongs in BUILD_LOG.md.
- The recent-shifts list holds the last 3 sessions at most, one line each, newest first. When adding a fourth, delete the oldest. Nothing is lost: anything that mattered from a dropped session is already in BUILD_LOG.md.
- Record the tool and model you believe you are, with the date. This is best-effort metadata, not an audit trail; models sometimes misreport their identity.
- This file never grows and is never archived. Full history and the reasoning behind decisions live in BUILD_LOG.md.

## Current state (max 20 lines, overwrite each session)
- What works: [nothing yet, project just initialised]
- Half-built / in flight: [nothing]
- Settled, do not relitigate: [nothing yet]
- Watch out for: [nothing yet]

## Recent shifts (max 3, newest first, one line each)
* **[Insert date]** — [Tool, model] — Repository initialised with the Prime Directive Framework.
